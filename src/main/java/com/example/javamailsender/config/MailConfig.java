package com.example.javamailsender.config;

import org.springframework.context.annotation.Configuration;

/**
 * Mail Configuration
 * (SMTP properties are in application.properties)
 */
@Configuration
public class MailConfig {
    // Custom mail configuration can be added here if needed
}
